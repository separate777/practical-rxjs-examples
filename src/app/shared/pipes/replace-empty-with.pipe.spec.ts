import { ReplaceEmptyWithPipe } from './replace-empty-with.pipe';

describe('ReplaceEmptyWithPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceEmptyWithPipe();
    expect(pipe).toBeTruthy();
  });
});
